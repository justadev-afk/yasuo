{
  "compilerOptions": {
    // Environment & language target
    "lib": ["ESNext"],
    "target": "ESNext",
    "module": "ESNext",
    "moduleDetection": "force",
    "types": ["bun"],

    // Module resolution (bundled by tsup for publishing)
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    // tsup injects a `baseUrl` for its dts rollup; TS 6.0 flags that as
    // deprecated (removed in 7.0). Acknowledge it so `bun run build` stays green.
    "ignoreDeprecations": "6.0",

    // Strictness — restrictive by design
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true
  },
  "include": ["src", "test", "examples"],
  "exclude": ["node_modules", "dist"]
}
